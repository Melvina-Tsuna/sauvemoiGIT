import 'package:flutter/cupertino.dart';

class Post extends StatelessWidget {

  final String from;
  final String text;

  final bool me;

  const Post({Key key, this.from, this.text, this.me}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container();
  }
  }